  梳理
1.geometry的功能是接受深度图和ERP（有颜色信息），然后先类内先构造一个球体用于计算（默认以传入BBoxXYWH的左上角为中心点）
  输入输出：1.给出一个原图点，以之为圆心的某大小的FoV，转化为BBox

2.核心算法：1.预测下一帧物体位置的算法（问题6中） 2.Hit算法的适配和优化（目前可能采取问题3解决） 


  问题：
1.DTC中读取深度信息只读取深度摘要问题：
  （1）深度摘要只能给出一个场景离摄影机的距离信息，虽然有用但是不能直接用来推测物体的运动轨迹
  （2）只给深度摘要那么就忽略了大量的有效信息，并且DTC是唯一处理深度的部分，这样不能充分利用信息做到有效算法规划
 改变建议：
  （1）保留深度摘要，但是额外增加读取BFoV的区域的深度，也就是读取物体或猜测物体位置的深度
  （2）DTC处理深度数据，可以从前后两帧图片的 整体深度变化 和 物体深度变化 两个数据预测物体运动的方向 速率，
       结合360VOT给出的BFoV方向变化率，结合推测物体运动方向，摄影设备移动方向，做轻量级物理建模以进行预测
  （3）物体附近深度信息极其关键，由于立体物体在空间中离摄影设备的距离与背景往往有明显差异，所以可以通过深度图来
       帮助预测物体形状和边界（用来帮助划定边界尤其重要！！）这个应当专门设计算法加以利用
  （4）查看是否能利用技术实现背景图加深度图进行立体空间数学建模，如果可以，看能不能给出轻量级算法

2. geometry中 
      （1）Fov转化为BBox的算法，怎么避免变形
      （2）是否给出的点是查找的FoV的原心，需不需要给出所要的框的大小，这个转化的过程是否需要一个算法
      （3）既然是先给出一个点返回一个数据，那我怎么知道一开始要哪个点呢，线路是不是先DTC计算出几个大概的位置，然后给geometry，geometry返回Box之后给HiT查询
      （4）当数据有深度图的时候，geometry必须把深度图传入，并且和BRT一一对应，然后当返回Box的时候必须把对应的深度图也返回，给Hit处理

3. 颜色信息是否会在geometry的处理中丢失，能否传递到Hit中，能不能把一个图片的深度信息加入到图片中，或者任何其他方式，让Hit将两种信息一起处理！！！
    本点极其重要，本项目处理信息的时候应当同时处理颜色和深度信息，因为有可能颜色相近但是深度差距明显，深度相近但是颜色差距明显，所以处理信息必须运用
     可能解决路径（给出三种方案评估和可行性）：
                   （1）做两套Hit，当两者有一个结果置信率明显高于另一个就用这个（可行性原因，以每个像素的深度构成的向量本身也可以当成图片）
                   （2）做三套Hit，分别是纯粹颜色图片，纯粹深度图片，然后以某种w加权后的颜色深度相加图片，三者选择置信率最高的
                   （3）在（1）的基础上不是通过简单选择置信度高，而是选择用全连接层等方式用神经网络进行结合判断，训练出两者最佳权重比

4.总体来说要重新设计深度图在系统中的权重，将深度图放在和颜色图相同的地位，比如geometry必须传送图像对应的深度信息给DTC或者HIT，而凡是能传递颜色图的路径
    都需要能传递对应的深度图，例如DTC和HIT之前能获得颜色图的地方都应该能获得深度图

5.所有有深度图的地方都需要做 对没有深度图测试集的适配，也就是 本项目能处理 1.同时有颜色图和深度图的数据集 2.只有颜色图没有深度图的数据集（因为我现在还不知道测试集的结构）

6.可能的预测下一帧位置算法 （1）简单的用前n帧位置中心点来进行简单的二维机器学习来计算可能路径与下一个点
                           （2）能不能实现用球面坐标加深度信息直接模拟出三维空间，然后根据三维空间的前n帧位置信息机器学习预测可能路径
                           （3）能不能实现用球面信息加深度信息，不专门三维建模，运用某种加权的机器学习模型来计算预测路径