现在的问题：
  1.到底将DepthToRGB 放在tracker还是geometry，方案一:一开始传入geometry的时候直接转化为RGB，然后tracker只接受rawRGB和DepthRGB；
     方案二：是现在的 geometry传给tracker一个深度的局部深度表，然后 tracker自己转化为颜色图并且丢给Hit
     现在思路，既然已经实现了方案二，如果修改成方案一不能有明显的效率提升就保持方案一
  
  2.在DTC中，截取图片应该尽可能的大，避免直接丢失物体，或者只给tracker局部图，让他误以为找寻物体中的某一个部分才是要找寻的东西，
     这里可以设计成一个超参数，例如DTC要求geometry给出的局部图大小，应该至少为首框目标框的4倍（即宽和高各乘以2），这个2可以作为
     超参数动态调整，且 ：尽量在寻找的过程中，以上一帧给出来的预测框大小和初始框大小两个数据共同考虑下一帧大小，避免物体在运动中
     变大后局部图仍较小

  3.注意，因为追踪物体过程中可能会有几帧丢失物体或者物体完全消失，在设计tracking,uncertain recovering lost的链条的时候，不能一张图
   卡住就一直在这张图上面反复寻找，例如可以设计成路径tracking的置信度低于x之后，进入uncertain和后面Lost之类的都需要在扩大视野重新搜索
   上一张的同时，也进行对应的下一帧的预测，并如果这一轮结束还在uncertain或Lost，进入下一轮还可以给出下下帧的预测，如果后面帧中有了极高
    置信度的图片，同样可以判定为找回，并且重新回归tracking的路径，中间处于uncertain的部分的帧给出来的局部框和输出暂时选择置信度最高的作为输出；
    如果前面帧重新找回时获得了极高置信度并回到tracking，则从该帧该结果重新开始后面帧的预测，之前uncertain时后面帧的预测结果放弃重新按流程计算

  4.DTC向Geometry给出下一帧的ViewSpec需求的时候，列表应当有 1.以上一帧局部框中心点为核心，每xy轴圆心角120°，z轴（-90到+90）的可以凑成
    一整个完整的视野的中左右三张图（避免丢失物体）2.给出按照超参数和追踪算法给出的要求的n张图。3.也就是说不管置信度怎么样，即使在tracking状态下，
    每一帧也必须要有多张图片共同参考，特别是需要有三张全景图，避免局部过拟合，丢失了寻找的物体
    注意：1.每一帧图片的追踪都可以同时先给出并计算多张图片，并根据这些图片得到的局部框和置信度，考虑置信度，不同图片预测结果的重合率等综合给出最后的预测框 
    2.要写一个超参数利于置信度低于40%的数据直接忽略或者在给出最后参考框的时候不计入考虑

  5.确保在我的tracker中的预测置信度的计算中，深度转化的RGB图片Hit结果一定是参与了考量的，并且Raw图片RGB，深度图片RGB，以及其他信息的融合头是
     可以再训练的

  6.本项目中 tracker融合头会给出一个预测框和置信度 命名为单图置信度 和 单图预测框 ；DTC综合了这一帧所得的所有图，最后给出的 局部框和置信度 命名为
     单帧预测框和多帧预测框